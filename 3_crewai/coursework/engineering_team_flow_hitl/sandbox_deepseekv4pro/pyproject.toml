[project]
name = "portfolio-manager"
version = "0.1.0"
description = "Trading simulation account management system"
requires-python = ">=3.10"
dependencies = [
    "gradio",
    "pytest",
]

[tool.pytest.ini_options]
testpaths = ["."]
